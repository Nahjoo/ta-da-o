{{ form_start(form) }}
    <div id="college" class="from-group">
        {{ form_row(form.name) }}
    </div>
    <div id="college" class="from-group">
        {{ form_row(form.college) }}
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
